{
  "name": "thehand",
  "version": "0.0.1",
  "description": "This is a distributed dns service",
  "main": "dist/cli.js",
  "bin": {
    "thehand": "dist/cli.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:cli && npm run build:service && chmod +x dist/cli.js && npm link",
    "build:cli": "esbuild src/cli.ts --bundle --platform=node --outfile=dist/cli.js",
    "build:service": "esbuild src/service.ts --bundle --platform=node --outfile=dist/service.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/fs-extra": "^9.0.13",
    "@types/node": "^18.11.8",
    "esbuild": "^0.15.12",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@honojs/node-server": "^0.1.0",
    "cmd-ts": "^0.11.0",
    "fs-extra": "^10.1.0",
    "hono": "^2.3.2",
    "pkg": "^5.8.0"
  }
}
